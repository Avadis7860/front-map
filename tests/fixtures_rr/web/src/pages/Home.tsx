import Button from '@/components/ui/Button'

/** Écran d'accueil — consomme Button (import par défaut) + un token littéral. */
export default function Home() {
  return <Button style={{ color: 'var(--color-accent-500)' }}>hi</Button>
}
